#!/usr/bin/bash

CYAN='\033[1;36m'
NO_COLOR='\033[0m'
LABEL="db-seed"
printf "${CYAN}== ${LABEL}${NO_COLOR}\n"

seed_path="backend-flask/db/sql/seed.sql"
echo "Loading seed data from: ${seed_path}"

cat $seed_path | docker-compose exec -T db psql --username "$PG_DATABASE" --dbname "$PG_DATABASE"